<nav class="workspace-shortcuts" aria-label="Dashboard shortcuts">
    <% shortcuts.forEach((shortcut, index) => { %>
        <a href="<%= shortcut.href %>"><span class="shortcut-number"><%= String(index + 1).padStart(2, '0') %></span><span><%= shortcut.label %></span></a>
    <% }) %>
</nav>
